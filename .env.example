# Copy to `.env` and adjust values. Used for configuration (future LLM keys, etc.).
#
# Application
APP_NAME=AI CRM Follow-up Automation
APP_ENV=development

# Future: OpenAI or other LLM provider (not used in v1 placeholder implementation)
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
